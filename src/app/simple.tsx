// Simple test page to replace complex root page temporarily
export default function SimplePage() {
  return (
    <html>
      <body>
        <h1>TCG Simple Test</h1>
        <p>This is a minimal page to test if Next.js routing works.</p>
      </body>
    </html>
  );
}
